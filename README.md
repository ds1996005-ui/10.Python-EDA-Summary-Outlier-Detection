# 10.Python-EDA-Summary-Outlier-Detection
Gaining Practical EDA skill + outlier handling

1.Load dataset and check .shape, .info(), .head().

2.Generate descriptive statistics using .describe().

3.Compute missing value % for each column.

4.Plot distributions (histogram + boxplot).

5.Detect outliers using IQR method.

6.Create outlier flag column.

7.Handle outliers (remove/cap) with reason.

8.Create correlation matrix and interpret top correlations.

9.Export cleaned dataset
